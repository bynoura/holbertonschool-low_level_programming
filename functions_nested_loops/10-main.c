#include <stdio.h>
#include "main.h"

/**
 * main - Entry point to test the add function
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    int result;

    result = add(89, 9);
    printf("%d\n", result);

    return (0);
}
